# GUI Tail - プロジェクト仕様書

## 概要
LinuxのtailコマンドをWindows GUIで再現するデスクトップアプリケーション。
Tauriフレームワークを使用し、ファイルのリアルタイム監視と表示を行う。

## 技術スタック
- **フレームワーク**: Tauri v2
- **バックエンド**: Rust
- **フロントエンド**: HTML/CSS/TypeScript (React)
- **ファイル監視**: notify crateによるOSネイティブ監視
- **データ永続化**: JSONファイル（DBは使わない）

## 機能仕様

### 1. ファイル監視・表示
- 指定ファイルを監視し、追記があった場合にリアルタイムで新しい行を表示する
- 新しい行は画面下部に追加される（最新行が一番下）
- 初回読み込み時はファイル末尾からバッファ数分の行を読み込む

### 2. バッファ管理
- 表示行数のバッファ上限を設定可能（デフォルト: 10,000行）
- バッファ上限を超えた場合、古い行（上部）から削除する
- 削除された行のメモリは確実に解放する（メモリリーク防止）

### 3. 履歴機能
- 一度開いたファイルのパスを履歴として保存する
- 履歴からファイルを選択してすぐに開ける
- 履歴はJSONファイルとしてアプリデータディレクトリに永続化する
- DBは使用しない

### 4. テキスト選択・コピー
- マウスドラッグによるテキスト選択が可能（複数行対応）
- 選択中のテキストはCtrl+Cでクリップボードにコピー可能
- テキスト選択中（ドラッグ中）は自動スクロール（最新行追従）を一時停止する
  - 理由: 自動スクロールが動くとドラッグ選択が崩れ、コピーできなくなるため
- 選択解除後に自動スクロールを再開する

### 5. マルチインスタンス
- アプリケーションのインスタンスを複数同時に起動可能
- 各インスタンスがそれぞれ別のファイルを監視できる
- シングルインスタンス制限は設けない

## 非機能要件

### メモリ管理
- バッファ超過分のテキストデータは速やかにメモリから解放する
- Rust側ではStringのdrop、フロントエンド側ではDOM要素の削除で対応
- 長時間�kind動でもメモリ使用量が際限なく増加しないこと

### パフォーマンス
- ファイル変更検知から表示までの遅延を最小限にする（目標: 100ms以内）
- 大量行の一括追記にも対応する（バッチ処理で描画負荷を軽減）

## アーキテクチャ

### バックエンド（Rust / Tauri）
- `notify` crateでファイル変更を監視
- 変更検知時にファイルの差分（新規追加行）を読み取り
- 読み取った行をフロントエンドにイベントとして送信
- 履歴ファイルの読み書き（JSON形式）

### フロントエンド（React + TypeScript）
- Tauriイベントリスナーで新規行を受信
- バッファ管理（state内の行配列を上限に保つ）
- 仮想スクロール or DOM直接操作で大量行を効率的に表示
- テキスト選択状態の検知と自動スクロール制御

### ファイル構成（予定）
```
gui-tail/
├── src-tauri/
│   ├── src/
│   │   ├── main.rs          # Tauriエントリポイント
│   │   ├── watcher.rs       # ファイル監視ロジック
│   │   ├── reader.rs        # ファイル読み取り（差分検出）
│   │   └── history.rs       # 履歴管理
│   ├── Cargo.toml
│   └── tauri.conf.json
├── src/
│   ├── App.tsx               # メインコンポーネント
│   ├── components/
│   │   ├── LogViewer.tsx     # ログ表示コンポーネント
│   │   ├── FileSelector.tsx  # ファイル選択UI
│   │   └── Settings.tsx      # バッファ数設定UI
│   ├── hooks/
│   │   └── useTailFollow.ts  # tail追従ロジック
│   └── main.tsx
├── CLAUDE.md
└── package.json
```

## セキュリティ: サプライチェーン攻撃対策

実装前に全ての依存パッケージについて以下を確認する。

### 確認対象
- **Rust crate** (Cargo.toml に記載するもの全て)
- **npm パッケージ** (package.json に記載するもの全て)

### 確認項目

#### 1. パッケージの正当性
- 公式リポジトリ（crates.io / npmjs.com）に存在するか
- パッケージ名のタイポスクワッティングでないか（例: `notfy` vs `notify`）
- 作者・メンテナが信頼できるか（GitHub stars、メンテナ実績、所属組織）

#### 2. メンテナンス状況
- 最終更新日が直近1年以内か（放置されたパッケージは脆弱性が放置されやすい）
- 既知の脆弱性（CVE）が報告されていないか
- 依存ツリーが肥大化していないか（不要な推移的依存を持ち込まない）

#### 3. 安全なインストール手順
依存パッケージのインストール時にpostinstallスクリプト等で悪意あるコードが実行されるリスクを防ぐため、以下の手順を厳守する。

**npm (postinstallスクリプトによる即時感染リスクあり)**:
1. `npm install --ignore-scripts` でスクリプト実行を抑止してダウンロードのみ
2. `npm audit` で既知の脆弱性をチェック
3. 問題なければ `npm rebuild` でスクリプトを実行

**Rust (ビルド時にbuild.rsが実行されるリスクあり)**:
1. `cargo generate-lockfile` でロックファイル生成のみ（ビルドせず）
2. `cargo audit` で既知の脆弱性をチェック
3. 問題なければ `cargo build` でビルド実行

#### 4. 継続的な検査
- CI/CD導入時は `cargo audit` と `npm audit` を自動実行する

#### 5. 依存の最小化方針
- 必要最小限のcrateとnpmパッケージのみ使用する
- 小さな機能のために大きな依存を追加しない
- 可能な限りTauri標準APIとRust標準ライブラリで実装する

### 使用予定パッケージと確認ステータス

| パッケージ | 種別 | 用途 | 確認状況 | 備考 |
|-----------|------|------|---------|------|
| tauri v2 | crate | アプリフレームワーク | 確認済 | tauri-apps org, 105k stars, 13M DL, 既知CVEは全てv1.x対象で修正済 |
| notify | crate | ファイル監視 | 確認済 | notify-rs org, 3.3k stars, 89M DL, CVE無し, CC0ライセンス |
| serde | crate | シリアライズ | 確認済 | dtolnay氏, 10.5k stars, 909M DL, CVE無し. タイポ注意: `serd`は悪意あるcrate(RUSTSEC-2023-0102) |
| serde_json | crate | JSONシリアライズ | 確認済 | dtolnay氏, 5.5k stars, 815M DL, CVE無し. `serde-json-wasm`は別crate |
| tokio | crate | 非同期ランタイム | 確認済 | tokio-rs org, 31.6k stars, 437M DL, 過去5件のadvisoryは全て修正済(重大度低) |
| react v19 | npm | UI フレームワーク | 確認済 | Meta, 127M DL/週, CVE-2025-55182等は19.2.1+で修正済. 19.2.4以上を使用 |
| @tauri-apps/api | npm | Tauri フロントエンドAPI | 確認済 | tauri-apps公式スコープ, CVE無し, タイポスクワッティングリスク低 |
| typescript | npm | 型付きJS | 確認済 | Microsoft, 55M DL/週, CVE無し, devDependencyのため攻撃面小 |
| vite v8 | npm (dev) | ビルドツール | 確認済 | Evan You氏, 78M DL/週, 過去CVEはdevサーバーのみ影響で全て修正済. 8.0.4以上を使用 |

> **確認日**: 2026-04-06
> **結論**: 全パッケージ確認済。実装開始可能。
> **注意事項**:
> - `serde` のタイポ `serd` は悪意あるcrate。スペルに注意
> - react は 19.2.4以上、vite は 8.0.4以上を使用すること
> - インストール時は必ず安全なインストール手順（セクション3）に従うこと
